using TechMarketing.Data; 
using Microsoft.EntityFrameworkCore;

namespace TechMarketing.Endpoints; 

public static class VendasEndpoint
{
    public static void MapVendasEndpoint(this WebApplication application)
    {
        

    }
}